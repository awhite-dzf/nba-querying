# nba-querying
This project consists of 13 SQL queries on an NBA database hosted locally through postgreSQL. These queries result in useful information that could be mentioned on a broadcast.

This project was done through a Jupyter notebook. All the queries are already run and their results are shown in the cells.

Dataset: https://www.kaggle.com/datasets/wyattowalsh/basketball?resource=download 
